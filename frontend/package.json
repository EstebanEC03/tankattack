{
  "name": "tank-attack-frontend",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Frontend Vite + TypeScript + Canvas para Tank-Attack",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5173",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.10"
  }
}
